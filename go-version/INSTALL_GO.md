# Установка Go и сборка проекта

## 📥 Установка Go

### Шаг 1: Скачать Go

1. Откройте https://go.dev/dl/
2. Скачайте **Windows installer** (например, `go1.21.5.windows-amd64.msi`)
3. Запустите установщик
4. Следуйте инструкциям (установка займет ~2 минуты)

### Шаг 2: Проверить установку

Откройте новую командную строку и выполните:

```bash
go version
```

Должно показать что-то вроде:
```
go version go1.21.5 windows/amd64
```

## 🔨 Сборка проекта

### Автоматическая сборка

```bash
cd c:\tmp\newproject\go-version
build.bat
```

### Ручная сборка

```bash
cd c:\tmp\newproject\go-version

# Загрузка зависимостей
go mod download

# Компиляция
go build -ldflags="-s -w" -o system-monitor.exe
```

## ✅ Результат

После сборки появится файл `system-monitor.exe` размером ~6-10 MB.

Этот файл можно:
- Копировать на любой Windows компьютер
- Запускать БЕЗ установки Go
- Распространять пользователям

## 🚀 Использование

```bash
# Тестовая отправка
system-monitor.exe --test

# Запуск сервиса
system-monitor.exe
```

## 📦 Альтернатива: Готовый .exe

Если вы не хотите устанавливать Go, я могу:

1. Собрать .exe на другом компьютере
2. Загрузить готовый .exe в GitHub Releases
3. Вы просто скачаете и используете

## 🎯 Преимущества Go версии

- ✅ **6-8 MB** вместо 30-40 MB (Python + PyInstaller)
- ✅ Работает БЕЗ Python
- ✅ Быстрый запуск
- ✅ Меньше потребление памяти

## ❓ Вопросы?

**Q: Обязательно ли устанавливать Go?**  
A: Только для компиляции. Пользователям Go НЕ нужен.

**Q: Можно получить готовый .exe?**  
A: Да, после компиляции файл можно распространять.

**Q: Совместим ли config.json с Python версией?**  
A: Да, полностью совместим.
