# Интерактивный режим - Инструкция

Руководство по использованию интерактивного Telegram бота для получения информации по запросу.

## 🎯 Возможности

### Dual Mode

Бот работает в **двух режимах одновременно**:

1. **Scheduled Mode** - автоматическая отправка отчетов по расписанию (08:00)
2. **Interactive Mode** - ответы на ваши команды в реальном времени

### Команды

| Команда | Описание |
|---------|----------|
| `/info` | Получить полный отчет о выбранном компьютере |
| `/status` | Краткий статус всех компьютеров |
| `/help` | Справка по командам |

---

## ⚙️ Настройка

### 1. Обновите config.json

Добавьте новые поля:

```json
{
  "computer_id": "office-main",
  "computer_name": "Офис - Главный",
  "telegram_token": "ваш_токен",
  "chat_id": "ваш_chat_id",
  "enable_polling": true,
  ...
}
```

**Поля:**
- `computer_id` - уникальный ID (латиница, без пробелов)
- `computer_name` - отображаемое имя (любые символы)
- `enable_polling` - включить интерактивный режим (`true`/`false`)

### 2. Имена для каждого компьютера

**Компьютер 1:**
```json
{
  "computer_id": "office-main",
  "computer_name": "Офис - Главный"
}
```

**Компьютер 2:**
```json
{
  "computer_id": "server-1",
  "computer_name": "Сервер 1"
}
```

**Компьютер 3:**
```json
{
  "computer_id": "home-pc",
  "computer_name": "Домашний ПК"
}
```

> ⚠️ **Важно**: `computer_id` должен быть уникальным для каждого компьютера!

---

## 🚀 Использование

### Команда /info

1. Отправьте `/info` в Telegram
2. Бот покажет кнопки всех активных компьютеров:

```
Выберите компьютер:
┌──────────────┬──────────────┐
│ Офис - Гл... │ Сервер 1     │
├──────────────┼──────────────┤
│ Домашний ПК  │ Сервер 2     │
└──────────────┴──────────────┘
```

3. Нажмите на кнопку нужного компьютера
4. Получите полный отчет

### Команда /status

Показывает краткую информацию обо всех компьютерах:

```
📊 Статус компьютеров:

✅ Офис - Главный - Online
   Последняя активность: 2m назад

✅ Сервер 1 - Online
   Последняя активность: 5m назад

❌ Домашний ПК - Offline
   Последняя активность: 2h назад
```

**Статусы:**
- ✅ **Online** - компьютер активен (< 10 минут без связи)
- ❌ **Offline** - компьютер не активен (> 10 минут)

---

## 🔧 Технические детали

### Как это работает

1. **Регистрация**: При запуске каждый компьютер регистрируется в памяти бота
2. **Polling**: Бот каждые 30 секунд проверяет новые сообщения от пользователя
3. **Обработка**: При получении команды бот показывает список компьютеров
4. **Ответ**: Нужный компьютер отправляет свой отчет

### Потребление ресурсов

| Параметр | Значение |
|----------|----------|
| RAM | +10 MB (для polling) |
| Размер .exe | +2 MB |
| Сетевой трафик | ~1 запрос / 30 сек |

### Лимиты

- **Максимум компьютеров**: 10 (по 2 кнопки в ряд)
- **Timeout**: 30 секунд между проверками
- **Offline порог**: 10 минут без активности

---

## 🎨 Примеры конфигураций

### Вариант 1: Только расписание (без команд)

```json
{
  "computer_name": "Мой ПК",
  "enable_polling": false,
  ...
}
```

**Результат**: Отчеты только по расписанию, команды не работают.

### Вариант 2: Только команды (без расписания)

```json
{
  "computer_name": "Мой ПК",
  "schedule_time": "",
  "enable_polling": true,
  ...
}
```

**Результат**: Отчеты только по запросу, автоматических нет.

### Вариант 3: Оба режима (рекомендуется)

```json
{
  "computer_name": "Мой ПК",
  "schedule_time": "08:00",
  "enable_polling": true,
  ...
}
```

**Результат**: Автоматические отчеты + ответы на команды.

---

## ❓ FAQ

### Как добавить новый компьютер?

1. Установите приложение на новый компьютер
2. Настройте `config.json` с уникальным `computer_id`
3. Запустите `system-monitor.exe`
4. Компьютер автоматически появится в меню `/info`

### Что если 2 компьютера с одинаковым ID?

Один из них перезапишет другой. Используйте уникальные ID!

### Можно ли изменить имя компьютера?

Да, измените `computer_name` в `config.json` и перезапустите.

### Как отключить интерактивный режим?

Установите `"enable_polling": false` в config.json

### Почему компьютер показывает Offline?

- Не запущен `system-monitor.exe`
- Прошло более 10 минут без активности
- Нет интернет-соединения

---

## 🔐 Безопасность

**Все компьютеры используют ОДИН токен бота и ОДИН chat_id:**

- ✅ Простая настройка
- ✅ Все отчеты в одном чате
- ⚠️ Все компьютеры должны иметь доступ к одному боту

**Альтернатива** (для разных пользователей):
- Используйте разные боты для разных групп компьютеров
- Каждая группа = свой токен + chat_id

---

## 📊 Формат отчета

При нажатии кнопки компьютера вы получите:

```
📊 Отчет о состоянии системы

🖥️ Компьютер: Офис - Главный
🕐 Время: 05.12.2025 23:00:00

🌐 Сеть:
├ Имя хоста: OFFICE-PC
├ Локальный IP: 192.168.1.100
└ Внешний IP: 93.123.45.67

💻 Процессор:
├ Ядер: 8
└ Загрузка: 23.5%

... и далее полная информация
```

---

**Автор**: Serik Muftakhidinov  
**Версия**: 1.0.0  
**Лицензия**: MIT
