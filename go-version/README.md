# System Monitor - Go Version

Компактная версия System Monitor на Go с размером исполняемого файла ~6-8 MB.

## 🎯 Особенности

- ✅ **Маленький размер**: 6-8 MB (вместо 30-40 MB Python версии)
- ✅ **Standalone**: Работает БЕЗ установки Python или других зависимостей
- ✅ **Быстрый**: Запуск за миллисекунды
- ✅ **Эффективный**: Потребление RAM ~10 MB
- ✅ **Полный функционал**: CPU, RAM, диски, процессы, IP, Telegram
- ✅ **Интерактивный режим**: Ответы на команды `/info`, `/status` и меню выбора ПК

## 📖 Документация

- [Инструкция по установке Go](INSTALL_GO.md)
- [Интерактивный режим и команды](INTERACTIVE.md)

## 📋 Требования

### Для пользователя

**НИЧЕГО!** Просто скопируйте `system-monitor.exe` и `config.json` - всё работает.

### Для разработчика (сборка из исходников)

- Go 1.21 или выше: https://go.dev/dl/

## 🚀 Быстрый старт

### 1. Настройка конфигурации

Отредактируйте `config.json`:

```json
{
  "telegram_token": "ВАШ_ТОКЕН",
  "chat_id": "ВАШ_CHAT_ID",
  "schedule_time": "08:00"
}
```

### 2. Тестовая отправка

```bash
system-monitor.exe --test
```

### 3. Запуск сервиса

```bash
system-monitor.exe
```

Сервис будет отправлять отчеты ежедневно в 08:00.

## 🛠️ Сборка из исходников

```bash
cd go-version
build.bat
```

Результат: `system-monitor.exe` (~6-8 MB)

### Ручная сборка

```bash
go mod download
go build -ldflags="-s -w" -o system-monitor.exe
```

Флаги:
- `-ldflags="-s -w"` - удаление отладочной информации для уменьшения размера

## 📝 Использование

### Команды

```bash
# Запуск в режиме службы
system-monitor.exe

# Тестовая отправка
system-monitor.exe --test

# Показать версию
system-monitor.exe --version

# Указать другой конфиг
system-monitor.exe --config путь/к/config.json
```

### Установка как Windows служба

Используйте `install_service.bat` из корневой папки проекта.

## 🏗️ Архитектура

```
go-version/
├── main.go              # Точка входа
├── config/              # Управление конфигурацией
├── monitor/             # Сбор системных данных
│   ├── system.go        # CPU, RAM, диски, процессы
│   └── network.go       # IP адреса
├── telegram/            # Telegram интеграция
│   └── bot.go          # Отправка сообщений
└── scheduler/           # Планировщик
    └── scheduler.go    # Ежедневная отправка
```

## 📦 Зависимости

- `github.com/shirou/gopsutil/v3` - системный мониторинг
- `github.com/go-co-op/gocron` - планировщик задач

Все зависимости компилируются в один .exe файл!

## 🆚 Сравнение с Python версией

| Характеристика | Python | Go |
|----------------|--------|-----|
| Размер | 30-40 MB | 6-8 MB |
| Зависимости | Python 3.8+ | Нет |
| Запуск | ~1000 ms | ~10 ms |
| RAM | ~50 MB | ~10 MB |
| Установка | setup.bat | Просто копировать |

## 🔧 Конфигурация

Все настройки в `config.json`:

```json
{
  "telegram_token": "токен бота",
  "chat_id": "ID чата",
  "schedule_time": "08:00",
  "monitor_all_disks": true,
  "language": "ru",
  "log_file": "monitor.log"
}
```

## 📊 Пример отчета

Точно такой же как в Python версии - с IP, CPU, RAM, дисками и процессами!

## ❓ FAQ

### Нужен ли Python?

**Нет!** Go версия - это standalone исполняемый файл.

### Работает ли на старых Windows?

Да, работает на Windows 7 и выше.

### Могу ли я использовать config.json из Python версии?

Да, формат полностью совместим.

### Как обновить?

Просто замените `system-monitor.exe` новой версией.

## 📄 Лицензия

MIT License

Copyright (c) 2025 Serik Muftakhidinov

## 👤 Автор

**Serik Muftakhidinov**

Разработано в 2025 году
