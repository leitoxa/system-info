# Инструкция по публикации на GitHub

Пошаговая инструкция для создания репозитория и публикации проекта System Monitor Bot на GitHub.

---

## 📋 Предварительные требования

1. Установлен [Git](https://git-scm.com/)
2. Есть аккаунт на [GitHub](https://github.com/)

---

## 🚀 Шаг 1: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс

1. Откройте [GitHub](https://github.com/) и войдите в аккаунт
2. Нажмите кнопку **"New"** (зеленая кнопка) или **"+"** → **"New repository"**
3. Заполните форму:
   - **Repository name**: `system-monitor-bot` (или любое другое имя)
   - **Description**: `Telegram bot for system monitoring with daily reports`
   - **Visibility**: 
     - ✅ **Public** - если хотите открытый доступ
     - ✅ **Private** - если репозиторий только для вас
   - **НЕ ставьте галочки**:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
     
     *(мы уже создали эти файлы локально)*

4. Нажмите **"Create repository"**

5. **Скопируйте URL репозитория** (он будет выглядеть как):
   ```
   https://github.com/ВАШ_USERNAME/system-monitor-bot.git
   ```

---

## 🔧 Шаг 2: Инициализация локального репозитория

### Автоматический способ (рекомендуется)

1. Откройте командную строку в папке проекта:
   ```
   cd c:\tmp\newproject
   ```

2. Запустите скрипт инициализации:
   ```
   git_init.bat
   ```

3. Следуйте инструкциям на экране

### Ручной способ

Если `git_init.bat` не работает, выполните команды вручную:

```bash
# Инициализация репозитория
git init

# Настройка автора
git config user.name "Serik Muftakhidinov"
git config user.email "your.email@example.com"

# Добавление файлов
git add .

# Первый коммит
git commit -m "Initial commit: System Monitor Telegram Bot"
```

---

## 📤 Шаг 3: Подключение к GitHub и первый Push

### Вариант A: HTTPS (проще для начинающих)

1. Добавьте remote (замените URL на ваш):
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/system-monitor-bot.git
   ```

2. Переименуйте ветку в main (если нужно):
   ```bash
   git branch -M main
   ```

3. Отправьте код на GitHub:
   ```bash
   git push -u origin main
   ```

4. При первом пуше GitHub может запросить авторизацию:
   - **Username**: ваш GitHub username
   - **Password**: используйте **Personal Access Token** (не пароль от аккаунта!)

### Как создать Personal Access Token:

1. Откройте GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Настройки токена:
   - **Note**: `system-monitor-bot`
   - **Expiration**: выберите срок действия
   - **Scopes**: отметьте **`repo`** (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **СКОПИРУЙТЕ ТОКЕН** (он больше не будет показан!)
6. Используйте этот токен вместо пароля при push

### Вариант B: SSH (для опытных пользователей)

1. Настройте SSH ключи (если еще не сделали):
   ```bash
   ssh-keygen -t ed25519 -C "your.email@example.com"
   ```

2. Добавьте SSH ключ в GitHub (Settings → SSH and GPG keys)

3. Используйте SSH URL:
   ```bash
   git remote add origin git@github.com:ВАШ_USERNAME/system-monitor-bot.git
   git branch -M main
   git push -u origin main
   ```

---

## 🔄 Шаг 4: Регулярные обновления

После первоначальной настройки используйте автоматический скрипт для обновлений:

### Автоматический способ

```bash
git_push.bat
```

Скрипт:
- Покажет текущие изменения
- Запросит сообщение коммита (или создаст автоматическое)
- Создаст коммит
- Отправит изменения на GitHub

### Ручной способ

```bash
# Посмотреть изменения
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

---

## 📝 Рекомендации по работе с Git

### Хорошие практики коммитов

✅ **Хорошие сообщения коммитов:**
- `Add IP address monitoring feature`
- `Fix memory calculation bug`
- `Update README with setup instructions`
- `Improve error handling in Telegram API`

❌ **Плохие сообщения:**
- `update`
- `fix`
- `changes`

### Что НЕ коммитить

Файл `.gitignore` уже настроен на исключение:
- `venv/` - виртуальное окружение
- `*.log` - файлы логов
- `__pycache__/` - кеш Python

⚠️ **ВАЖНО**: Файл `config.json` с вашими токенами **по умолчанию КОММИТИТСЯ**!

Если вы хотите его исключить (рекомендуется для публичных репозиториев):
1. Откройте `.gitignore`
2. Раскомментируйте строку: `# config.json` → `config.json`
3. Создайте `config.example.json` с плейсхолдерами для других пользователей

---

## 🛠️ Полезные команды Git

```bash
# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git status
git diff

# Отменить изменения в файле (до коммита)
git checkout -- файл.py

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Посмотреть информацию о remote
git remote -v

# Обновить локальную копию с GitHub
git pull

# Клонировать репозиторий на другом компьютере
git clone https://github.com/ВАШ_USERNAME/system-monitor-bot.git
```

---

## 🎯 Быстрый чеклист

- [ ] Git установлен
- [ ] Репозиторий создан на GitHub
- [ ] URL репозитория скопирован
- [ ] Запущен `git_init.bat`
- [ ] Remote добавлен: `git remote add origin URL`
- [ ] Первый push выполнен: `git push -u origin main`
- [ ] Personal Access Token создан (для HTTPS)
- [ ] Проверка: код виден на GitHub

---

## ❓ Решение проблем

### Ошибка: "remote origin already exists"

```bash
git remote remove origin
git remote add origin НОВЫЙ_URL
```

### Ошибка: "failed to push some refs"

```bash
git pull --rebase origin main
git push
```

### Ошибка аутентификации (403)

- Убедитесь, что используете Personal Access Token вместо пароля
- Проверьте права токена (должен быть scope `repo`)

### Забыли добавить файл в .gitignore

```bash
# Удалить файл из Git (но не с диска)
git rm --cached config.json

# Добавить в .gitignore
echo config.json >> .gitignore

# Закоммитить
git add .gitignore
git commit -m "Add config.json to gitignore"
```

---

## 📚 Дополнительные ресурсы

- [Git документация](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Pro Git книга (бесплатно)](https://git-scm.com/book/ru/v2)

---

**Автор**: Serik Muftakhidinov  
**Лицензия**: MIT License
