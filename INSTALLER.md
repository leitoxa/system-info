# Создание установщика

Руководство по созданию и распространению установщика System Monitor Bot.

---

## 📦 Быстрое создание установщика

### Один клик

```bash
build_installer.bat
```

Скрипт создает два варианта:
1. **Python версия** (легкий установщик, требует Python): `output\system-monitor-bot-installer.zip`
2. **Go версия** (standalone, ничего не требует): `go-version\system-monitor-go.zip`

---

## 📋 Что включено в установщик

### Go версия (Рекомендуется)
| Файл | Описание |
|------|----------|
| `system-monitor.exe` | Готовый к запуску сервис (9 MB) |
| `config.json` | Настройки |
| `README.md` | Инструкция |
| `INTERACTIVE.md` | Описание команд бота |

### Python версия (Classic)
| Файл | Описание |
|------|----------|
| `setup.bat` | **Интерактивный установщик** с запросами настроек |
| `monitor.py` | Основной скрипт мониторинга |
| `requirements.txt` | Python зависимости |
| `run.bat` | Запуск сервиса |
| `install_service.bat` | Установка как Windows служба |
| `config-template.json` | Шаблон конфигурации |
| `README.md` | Полная документация |
| `LICENSE` | MIT лицензия |
| `INSTALL.txt` | Краткая инструкция по установке |

> **Примечание**: `config.json` с реальными токенами **НЕ** включается в установщик для безопасности.

---

## 🎯 Интерактивная установка

Новый `setup.bat` теперь запрашивает у пользователя:

### 1️⃣ Токен Telegram бота
```
Введите токен бота: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 2️⃣ Chat ID
```
Введите Chat ID: 987654321
```

### 3️⃣ Время отправки отчетов
```
Введите время (по умолчанию 08:00): 09:30
```

### Дополнительные опции:
- ✅ Тестовая отправка сообщения (yes/no)
- ✅ Установка как Windows служба (yes/no)

После ввода всех данных автоматически создается файл `config.json` с указанными параметрами!

---

## 🚀 Процесс установки для конечного пользователя

### Шаг 1: Распаковка

Распакуйте `system-monitor-bot-installer.zip` в любую папку, например:
```
C:\Program Files\SystemMonitor\
```

### Шаг 2: Запуск установщика

Запустите `setup.bat`

### Шаг 3: Следуйте инструкциям

Установщик проведет через все шаги:

```
╔════════════════════════════════════════════════════════════════╗
║   System Monitor - Telegram Bot                               ║
║   Установщик с интерактивной настройкой                       ║
╚════════════════════════════════════════════════════════════════╝

[1/6] Проверка Python...
[2/6] Создание виртуального окружения...
[3/6] Установка зависимостей...
[4/6] Создание конфигурационного файла...
[5/6] Тестовая отправка...
[6/6] Установка как служба Windows...
```

### Шаг 4: Готово!

Сервис настроен и готов к работе.

---

## 📤 Распространение

### Вариант 1: ZIP архив

Отправьте файл `output\system-monitor-bot-installer.zip`:
- По email
- Через облачное хранилище (Google Drive, Dropbox)
- На USB флешке
- Через корпоративную сеть

### Вариант 2: GitHub Releases

1. Загрузите ZIP в GitHub:
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

2. Создайте Release на GitHub:
   - Перейдите: https://github.com/leitoxa/system-info/releases
   - Нажмите "Create a new release"
   - Выберите тег v1.0.0
   - Прикрепите `system-monitor-bot-installer.zip`
   - Опубликуйте

3. Пользователи смогут скачать по ссылке:
   ```
   https://github.com/leitoxa/system-info/releases/latest
   ```

---

## 🔄 Обновление версии

При внесении изменений:

1. Обновите код
2. Пересоберите установщик:
   ```bash
   build_installer.bat
   ```
3. Создайте новый release на GitHub с новой версией

---

## ⚙️ Системные требования

Минимальные требования, которые должны быть у пользователя:

| Компонент | Требование |
|-----------|------------|
| ОС | Windows 7/8/10/11 (64-bit) |
| Python | 3.8 или выше |
| RAM | 512 MB свободной памяти |
| Диск | 100 MB свободного места |
| Интернет | Для Telegram API |

---

## 📝 Инструкция для пользователя

После установки пользователь получает полностью готовый сервис:

### Запуск

**Тестовый режим:**
```bash
run.bat --test
```
Немедленная отправка отчета в Telegram.

**Постоянный режим:**
```bash
run.bat
```
Работа по расписанию (каждый день в указанное время).

**Как Windows служба:**
```bash
install_service.bat
```
Автоматический запуск при старте Windows.

### Настройка

Если нужно изменить настройки, отредактируйте `config.json`:
```json
{
  "telegram_token": "ВАШ_НОВЫЙ_ТОКЕН",
  "chat_id": "ВАШ_НОВЫЙ_CHAT_ID",
  "schedule_time": "10:00"
}
```

---

## 🛡️ Безопасность

### Что НЕ попадает в установщик:

- ❌ `config.json` с реальными токенами
- ❌ `*.log` файлы
- ❌ Виртуальное окружение `venv/`
- ❌ `__pycache__/` кеш файлы

### Рекомендации:

1. **Токены** - каждый пользователь вводит свои
2. **Chat ID** - уникальный для каждого
3. **config.json** - добавлен в `.gitignore`

---

## 📊 Размер установщика

Примерный размер ZIP архива: **~50-100 KB**

Содержит только исходный код и документацию. Python зависимости устанавливаются автоматически при запуске `setup.bat`.

---

## ❓ FAQ

### Нужен ли администратор для установки?

**Нет**, для базовой установки. 
**Да**, только для установки как Windows служба (`install_service.bat`).

### Можно ли установить без интернета?

**Нет**, требуется интернет для:
- Установки Python зависимостей (pip install)
- Работы Telegram API

### Как обновить установленный сервис?

1. Скачайте новую версию установщика
2. Распакуйте в ту же папку (с заменой)
3. Запустите `setup.bat`
4. Ваш `config.json` сохранится автоматически

---

## 🎓 Для разработчиков

### Изменение списка файлов в установщике

Отредактируйте `build_installer.bat`, секция "Копирование файлов":

```batch
copy НОВЫЙ_ФАЙЛ.txt %TEMP_DIR%\ >nul
```

### Кастомизация установщика

Отредактируйте `setup.bat`:
- Измените текст приветствия
- Добавьте новые запросы
- Измените путь установки по умолчанию

---

**Автор**: Serik Muftakhidinov  
**Лицензия**: MIT License
